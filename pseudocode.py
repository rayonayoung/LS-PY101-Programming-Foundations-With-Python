'''Write pseudo code for:

1) function that returns the sum of two numbers
2) a function that takes a list of strings, and returns a string 
that is all those strings concatenated together
3) a function that takes a list of integers, and returns a new 
list with every other element from the original list, starting 
with the first element. '''
'''
1) 
START

FUNCTION sum_of_two_numbers(num1, num2):
    RETURN num1 + num2
END FUNCTION

2)

START
FUNCTION concat_list_of_strings(lst):
    SET iterator = 0
    SET concat_strings = 0

    WHILE iterator < len(lst):
        concat_strings += lst(0)
        
'''